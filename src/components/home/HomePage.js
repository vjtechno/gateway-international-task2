import React from 'react';

const HomePage = () => (
  <div className="container">
    <div className="starter-template">
      <h1>Track Your Tasks</h1>
      <p className="lead">
        Use this app to track your tasks.<br />
        Built using React + Redux + Bootstrap
      </p>
    </div>
  </div>
);

export default HomePage;
